
#include <iostream>
#include <thread>
#include <mutex>

#include "MarketData.hpp"
std::mutex mtx;

int main(){
  std::thread c(getCandle("DXY");
  c.detach();

  while(true){
    std::lock_guard<std::mutex
    double* c = period[max].open;
    
    std::cout << "Open
  }

  return 0;
}
